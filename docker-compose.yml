services:
  kostenvergleich:
    build: .
    image: kostenvergleich-waermelv:latest
    container_name: kostenvergleich
    ports:
      - "8501:8501"
    volumes:
      - ./projekte:/app/projekte
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
